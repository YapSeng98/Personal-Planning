# Copy to .env and fill in after registering OAuth on the instance
# (servicenow/README.md §6). In dev, leave VITE_SN_BASE empty — the Vite
# proxy forwards /api and /oauth_token.do to dev405150.service-now.com.
VITE_SN_BASE=
VITE_SN_CLIENT_ID=
VITE_SN_CLIENT_SECRET=
